<UserControl x:Class="WizardApp.Views.Third"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True"
             Height="300" Width="500">
    <Grid>
        <StackPanel Orientation="Vertical">
            <TextBlock Text="Third View" FontSize="48" />
            <TextBlock FontSize="20">All Paramter</TextBlock>
            <TextBlock Margin="30,0,0,10" FontSize="20" Text="{Binding AllParameter}" />
            <TextBlock FontSize="20">Duplicate Parameter</TextBlock>
            <TextBlock Margin="30,0,0,10" FontSize="20" Text="{Binding DuplicateParameter}" />
        </StackPanel>
    </Grid>
</UserControl>
